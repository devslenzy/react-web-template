import { Helmet } from "react-helmet";

export const HomePage = () => {
    return (
        <div>
            <Helmet>
                <title>Home Page</title>
            </Helmet>
            <span>Hello, world!</span>
        </div>
    )
};
